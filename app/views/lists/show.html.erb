<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @list.Name %>
</p>

<p>
  <strong>Owner:</strong>
  <%= @list.user.try(:email) %>
</p>
<h2>
List items
</h2>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Duedate</th>
    </tr>
  </thead>

  <tbody>
<% ListItem.where("List_id = ?", @list.id).each do |li| %>
<tr> 
<td> <%=li.Name%></td>
<td> <%=li.Duedate%></td>

</tr>
<% end %>
  </tbody>
</table>
<%= link_to 'New List item', new_list_item_path(:list_id=>@list.id) %>


<%= link_to 'Edit', edit_list_path(@list) %> 
<%= link_to 'Back', lists_path %>
